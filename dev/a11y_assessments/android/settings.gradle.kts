// Minimal settings so IDE can sync this folder. Use JDK 17 for Gradle 8.x.
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "android"
